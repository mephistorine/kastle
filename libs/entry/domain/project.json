{
    "name": "domain-entry",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/entry/domain/src",
    "projectType": "library",
    "tags": ["domain:entry", "type:domain-logic"],
    "targets": {
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "libs/entry/domain/jest.config.ts"
            }
        }
    }
}
